# Cloudflare Pages: cross-origin isolation for the threaded engine
# (SharedArrayBuffer). Without it the app falls back to one thread.
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
  X-Content-Type-Options: nosniff
