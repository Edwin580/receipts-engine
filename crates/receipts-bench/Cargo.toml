[package]
name = "receipts-bench"
description = "Benchmarks on real snapshots (std timing; criterion is not approved yet)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
receipts-core.workspace = true
receipts-plan.workspace = true
receipts-exec.workspace = true
receipts-snapshot = { path = "../receipts-snapshot" }
anyhow.workspace = true

[lints]
workspace = true
